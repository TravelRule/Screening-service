PORT=4100

# Soroban RPC + the deployed attestation-registry contract ID (see the
# attestation-registry repo's docs/DEPLOYMENT.md to get one on testnet).
SOROBAN_RPC_URL=https://soroban-testnet.stellar.org
SOROBAN_NETWORK_PASSPHRASE="Test SDF Network ; September 2015"
ATTESTATION_REGISTRY_CONTRACT_ID=CBQHNAXSI55GX2GN6D67GK7BHVPSLJUGZQEU7WJ5LKR5PNUCGLIMAO4E

# Secret key of an address that has been added as an issuer on the registry
# (see attestation-registry's `add_issuer`). NEVER commit a real secret key —
# this is here only as a placeholder for local/testnet use.
ISSUER_SECRET_KEY=SXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX

# TODO: real sanctions-list data provider config goes here once you swap out
# the stub in src/screeningStub.js — e.g. OFAC_API_KEY, PEP_LIST_URL, etc.
